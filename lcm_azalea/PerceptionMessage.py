"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

import lcm_azalea.EKFState

class PerceptionMessage(object):
    __slots__ = ["timestamp", "state"]

    __typenames__ = ["int64_t", "lcm_azalea.EKFState"]

    __dimensions__ = [None, None]

    def __init__(self):
        self.timestamp = 0
        self.state = lcm_azalea.EKFState()

    def encode(self):
        buf = BytesIO()
        buf.write(PerceptionMessage._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">q", self.timestamp))
        assert self.state._get_packed_fingerprint() == lcm_azalea.EKFState._get_packed_fingerprint()
        self.state._encode_one(buf)

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != PerceptionMessage._get_packed_fingerprint():
            raise ValueError("Decode error")
        return PerceptionMessage._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = PerceptionMessage()
        self.timestamp = struct.unpack(">q", buf.read(8))[0]
        self.state = lcm_azalea.EKFState._decode_one(buf)
        return self
    _decode_one = staticmethod(_decode_one)

    def _get_hash_recursive(parents):
        if PerceptionMessage in parents: return 0
        newparents = parents + [PerceptionMessage]
        tmphash = (0xb482900348c5237b+ lcm_azalea.EKFState._get_hash_recursive(newparents)) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff) + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if PerceptionMessage._packed_fingerprint is None:
            PerceptionMessage._packed_fingerprint = struct.pack(">Q", PerceptionMessage._get_hash_recursive([]))
        return PerceptionMessage._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", PerceptionMessage._get_packed_fingerprint())[0]

