"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class UrControlPoints(object):
    __slots__ = ["num_control_points", "control_points"]

    __typenames__ = ["int32_t", "double"]

    __dimensions__ = [None, ["num_control_points", 7]]

    def __init__(self):
        self.num_control_points = 0
        self.control_points = []

    def encode(self):
        buf = BytesIO()
        buf.write(UrControlPoints._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">i", self.num_control_points))
        for i0 in range(self.num_control_points):
            buf.write(struct.pack('>7d', *self.control_points[i0][:7]))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != UrControlPoints._get_packed_fingerprint():
            raise ValueError("Decode error")
        return UrControlPoints._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = UrControlPoints()
        self.num_control_points = struct.unpack(">i", buf.read(4))[0]
        self.control_points = []
        for i0 in range(self.num_control_points):
            self.control_points.append(struct.unpack('>7d', buf.read(56)))
        return self
    _decode_one = staticmethod(_decode_one)

    def _get_hash_recursive(parents):
        if UrControlPoints in parents: return 0
        tmphash = (0x9458dc204044b6e9) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff) + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if UrControlPoints._packed_fingerprint is None:
            UrControlPoints._packed_fingerprint = struct.pack(">Q", UrControlPoints._get_hash_recursive([]))
        return UrControlPoints._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", UrControlPoints._get_packed_fingerprint())[0]

